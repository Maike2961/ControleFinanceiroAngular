spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/controle_db
    username: useroot
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
        hibernate.format_sql: true
        hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
