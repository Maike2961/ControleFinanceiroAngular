import { DateFormtPipe } from './date-formt.pipe';

describe('DateFormtPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormtPipe();
    expect(pipe).toBeTruthy();
  });
});
